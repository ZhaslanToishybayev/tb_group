# 🚀 Руководство по миграции на Neon

## ✅ Что уже готово

1. ✅ SQL скрипт создан: `apps/api/scripts/migrate-to-neon.sql`
2. ✅ Автоматический скрипт миграции: `apps/api/scripts/deploy-to-neon.sh`
3. ✅ Prisma схема готова

## 📋 Следующие шаги

### Шаг 1: Создайте проект в Neon

1. Перейдите на https://neon.tech
2. Зарегистрируйтесь (GitHub/Google)
3. Создайте новый проект:
   - **Database name**: `tb_group` (или любое другое имя)
   - **Region**: выберите ближайший (например, `Europe West (Ireland)`)
   - **Username**: оставьте `postgres`
   - **Password**: будет сгенерировано автоматически

4. После создания скопируйте **Connection String**:
   - Перейдите в раздел "Connection Details"
   - Выберите "Connection string"
   - Скопируйте строку формата:
     ```
     postgresql://postgres:xxxxxxxx@ep-xxxxxxx.neon.tech/tb_group?sslmode=require
     ```

### Шаг 2: Выполните миграцию

```bash
# Перейдите в папку API
cd apps/api

# Выполните миграцию
./scripts/deploy-to-neon.sh "ваш_connection_string_из_neon"
```

**Пример:**
```bash
./scripts/deploy-to-neon.sh "postgresql://postgres:password@ep-1234567890.neon.tech/tb_group?sslmode=require"
```

### Шаг 3: Обновите .env файл

Откройте `apps/api/.env` и замените DATABASE_URL:

```bash
# Замените на ваш connection string из Neon
DATABASE_URL="postgresql://postgres:password@ep-1234567890.neon.tech/tb_group?sslmode=require"
```

### Шаг 4: Обновите Vercel

1. Зайдите в ваш проект на Vercel
2. Перейдите в Settings → Environment Variables
3. Найдите `DATABASE_URL` и замените на connection string из Neon
4. Передеплойте проект

### Шаг 5: Обновите Railway (если используется)

1. Зайдите в ваш проект на Railway
2. Перейдите в Settings → Variables
3. Найдите `DATABASE_URL` и замените на connection string из Neon
4. Передеплойте

## 🎉 Готово!

После выполнения всех шагов:
- ✅ БД будет работать на Neon (никогда не "засыпает")
- ✅ Проект полностью перенесен
- ✅ Все функции работают

## 📊 Преимущества Neon

- ✅ **100% uptime** - никогда не спит
- ✅ **Автоматическое масштабирование** - масштабируется по требованию
- ✅ **Branch** - создавайте ветки БД как в Git
- ✅ **Free tier** - 500MB + 3 млрд чтений бесплатно
- ✅ **Отличная интеграция с Vercel**
- ✅ **Автоматические резервные копии**

## 🆘 Устранение неполадок

### Ошибка: "relation does not exist"
- Убедитесь, что миграция выполнена успешно
- Проверьте, что DATABASE_URL правильный

### Ошибка: "SSL connection required"
- Добавьте `?sslmode=require` к connection string

### Ошибка: "database does not exist"
- Убедитесь, что database name в connection string соответствует созданной БД в Neon

## 📞 Нужна помощь?

Если возникли вопросы:
1. Проверьте статус проекта в Neon Console
2. Убедитесь, что connection string скопирован полностью
3. Проверьте логи в Railway/Vercel

---

**Автоматизация**: Все скрипты уже настроены. Просто выполните команды выше!
