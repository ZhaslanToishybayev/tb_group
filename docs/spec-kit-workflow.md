# Spec Kit Workflow - Как это работает в нашем проекте

## Обзор

Spec Kit - это система для специфицированного управления разработкой, которая помогает организовать работу от идеи до реализации через структурированные документы и задачи.

## Структура Spec Kit

```
.specify/
├── memory/
│   └── constitution.md           # Конституция проекта (принципы, правила)
├── scripts/bash/
│   ├── common.sh                # Общие функции для всех скриптов
│   ├── check-prerequisites.sh   # Проверка предварительных условий
│   ├── create-new-feature.sh    # Создание новой фичи
│   ├── setup-plan.sh            # Настройка плана реализации
│   ├── spec-to-taskmaster.sh    # Синхронизация с Task Master
│   ├── spec-to-taskmaster-kilo.sh   # KILO Code версия синхронизации
│   └── spec-to-taskmaster-manual.sh # Ручная синхронизация
└── templates/
    ├── spec-template.md         # Шаблон спецификации
    ├── plan-template.md         # Шаблон плана реализации
    ├── tasks-template.md        # Шаблон задач
    ├── checklist-template.md    # Шаблон чек-листа
    └── agent-file-template.md   # Шаблон для AI агентов
```

## Основной workflow

### 1. Создание новой фичи

```bash
# Создаем новую фичу с описанием
.speckit.specify "Implement user authentication system"

# Это создаст:
# - Новую ветку: 001-user-authentication
# - Директорию: specs/001-user-authentication/
# - Файл спецификации: specs/001-user-authentication/spec.md
```

### 2. Настройка плана реализации

```bash
# Создаем план реализации
.speckit.plan

# Это создаст файл: specs/001-user-authentication/plan.md
```

### 3. Создание задач

```bash
# Генерируем задачи на основе спецификации
.speckit.tasks

# Это создаст файл: specs/001-user-authentication/tasks.md
```

### 4. Синхронизация с Task Master

```bash
# Синхронизируем задачи с Task Master
bash .specify/scripts/bash/spec-to-taskmaster.sh
```

## Структура документов фичи

```
specs/001-feature-name/
├── spec.md           # Спецификация с user stories
├── plan.md           # План реализации
├── tasks.md          # Задачи для разработки
├── research.md       # Исследования (опционально)
├── data-model.md     # Модель данных (опционально)
├── quickstart.md     # Инструкция по запуску (опционально)
└── contracts/        # API контракты (опционально)
```

## Шаблоны документов

### 1. Спецификация (spec.md)

Содержит:
- **User Scenarios & Testing** - пользовательские сценарии с приоритетами
- **Requirements** - функциональные требования
- **Success Criteria** - измеримые критерии успеха

Каждый user story должен быть:
- Независимо тестируемым
- Реализуемым как MVP
- Иметь приоритет (P1, P2, P3)

### 2. План реализации (plan.md)

Содержит:
- Архитектурные решения
- Технологический стек
- Структуру проекта
- Зависимости между компонентами

### 3. Задачи (tasks.md)

Содержит:
- Задачи, сгруппированные по user stories
- Фазы реализации (Setup, Foundational, User Stories)
- Зависимости и порядок выполнения
- Параллельные возможности

## Интеграция с Task Master

### Автоматическая синхронизация

```bash
# Стандартная синхронизация
bash .specify/scripts/bash/spec-to-taskmaster.sh

# С использованием KILO Code
bash .specify/scripts/bash/spec-to-taskmaster-kilo.sh
```

Процесс:
1. Читает `tasks.md` из текущей фичи
2. Конвертирует в PRD формат
3. Отправляет в Task Master для генерации задач
4. Создает задачи в Task Master

### Ручная синхронизация

```bash
# Ручное создание задач
bash .specify/scripts/bash/spec-to-taskmaster-manual.sh
```

## Текущий статус в нашем проекте

### ✅ Работает:
- Создание фичей и веток
- Генерация шаблонов документов
- Структура спецификаций
- Проверка предварительных условий

### ⚠️ Проблемы:
- **Автоматическая синхронизация** - проблемы с AI провайдерами
- **Codex CLI** - ошибки "no last agent message"
- **OpenRouter** - отсутствуют API ключи
- **Claude Code** - CLI не установлен

### 🔧 Решения:
- **Ручная синхронизация** через Task Master CLI
- **Обновление документации** вручную
- **Создание задач** через `task-master add-task`

## Принципы работы

### 1. User Story First
Каждая фича начинается с пользовательских сценариев, которые:
- Имеют четкий приоритет
- Независимо тестируемы
- Могут быть доставлены как MVP

### 2. Независимая реализация
User stories могут разрабатываться:
- Независимо друг от друга
- В параллель разными разработчиками
- С минимальными зависимостями

### 3. Тестирование сначала
Для каждого user story:
- Сначала пишутся тесты
- Тесты должны падать
- Затем реализуется функциональность

### 4. Инкрементальная доставка
Каждый user story добавляет ценность:
- Не ломает предыдущие story
- Может быть развернут независимо
- Демонстрирует прогресс

## Лучшие практики

### 1. Создание фичей
```bash
# Хорошо: конкретное описание
.speckit.specify "Add user authentication with OAuth2"

# Плохо: общее описание
.speckit.specify "User stuff"
```

### 2. User Stories
- Используйте приоритеты P1, P2, P3
- Описывайте независимые сценарии
- Включайте acceptance criteria

### 3. Задачи
- Группируйте по user stories
- Указывайте точные пути к файлам
- Отмечайте параллельные задачи [P]

### 4. Синхронизация
- Регулярно синхронизируйте с Task Master
- Обновляйте статусы задач
- Документируйте проблемы

## Интеграция с KILO Code

### Текущая попытка:
1. Создан скрипт `spec-to-taskmaster-kilo.sh`
2. Настроена конфигурация Task Master
3. Проблема: MCP провайдер не поддерживается

### Будущие решения:
1. Установка Claude Code CLI
2. Настройка правильных API ключей
3. Использование рабочих провайдеров

## Заключение

Spec Kit предоставляет структурированный подход к разработке, который помогает:
- Организовать работу от идеи до реализации
- Обеспечить независимость пользовательских сценариев
- Поддерживать инкрементальную доставку
- Интегрироваться с Task Master для управления задачами

Несмотря на текущие проблемы с синхронизацией, основная функциональность работает и может использоваться для структурированной разработки.